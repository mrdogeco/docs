{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "use-match",
  "title": "useMatch",
  "description": "One-shot fetch of a single match — no live updates, unlike useLiveMatch. Call again / remount to refresh.",
  "dependencies": [
    "@mrdoge/react",
    "@mrdoge/node",
    "server-only"
  ],
  "files": [
    {
      "path": "registry/mrdoge-ui/use-match/use-match.ts",
      "content": "// Re-exported from @mrdoge/react — fetched once per matchId, shared by\n// every component asking for it; a later mount reads the cache instead of\n// refetching.\nexport { useMatch, type UseMatchOptions } from \"@mrdoge/react\"\n",
      "type": "registry:hook"
    },
    {
      "path": "registry/mrdoge-ui/mrdoge-token-route/route.ts",
      "content": "import \"server-only\"\nimport { NextResponse } from \"next/server\"\nimport { MrDoge } from \"@mrdoge/node\"\n\nlet client: MrDoge | null = null\n\nfunction getClient() {\n  if (!client) {\n    client = new MrDoge({ apiKey: process.env.MRDOGE_ODDS_API_KEY! })\n  }\n  return client\n}\n\nexport async function POST() {\n  const { token, expiresAt } = await getClient().tokens.create()\n  return NextResponse.json({ token, expiresAt })\n}\n",
      "type": "registry:page",
      "target": "app/api/mrdoge/token/route.ts"
    }
  ],
  "envVars": {
    "MRDOGE_ODDS_API_KEY": ""
  },
  "type": "registry:hook"
}