---
title: useMatches
description: A plain list of matches, not limited to today's most-viewed ones. One-shot, not a subscription.
---

import { InstallTabs } from "@/components/docs/install-tabs"

Wraps `matches.list()`. Unlike
[useTrendingMatches](/docs/ui/mrdoge-sdk/hooks/use-trending-matches), not
limited to today's most-viewed matches, so it still returns results even
when nothing matching your filters happens to be trending right now.

## Installation

<InstallTabs name="use-matches" />

Installing this pulls in two files: the hook itself (a thin re-export
from [`@mrdoge/react`](/docs/ui/mrdoge-sdk/installation), which does the
real work) and a Next.js route at `app/api/mrdoge/token/route.ts` that
mints short-lived tokens using `@mrdoge/node` and your own
`MRDOGE_ODDS_API_KEY`. The browser client never sees the raw key.

## Usage

```tsx
import { useMatches } from "@/hooks/use-matches"

const matches = useMatches({ sports: ["soccer"], status: ["upcoming"], limit: 20 })
```

Takes a single options object, like every real SDK method it wraps
(`matches.list(params)`), not positional arguments. Only resolves *which*
matches; pair it with [useMatch](/docs/ui/mrdoge-sdk/hooks/use-match) to
actually fetch one.

<Callout title="status alone isn't enough">
  A match stuck on `"upcoming"` past its real kickoff (delayed, postponed,
  never updated) still matches that filter alone; pass `startDate` (and
  `endDate` for a range) to rule those out.
</Callout>

## Parameters

<TypeTable
  type={{
    regionIds: {
      description: "Restrict to matches in these regions.",
      type: "number[]",
    },
    sports: {
      description: 'Restrict to specific sports, e.g. ["soccer"]. Omit for all sports.',
      type: "string[]",
    },
    status: {
      description: 'Restrict to specific match statuses, e.g. ["upcoming"]. Omit for all statuses.',
      type: '("upcoming" | "live" | "completed")[]',
    },
    date: {
      description: "Single day, YYYY-MM-DD. Mutually exclusive with startDate/endDate.",
      type: "string",
    },
    startDate: {
      description: "Start of a date range, YYYY-MM-DD, inclusive.",
      type: "string",
    },
    endDate: {
      description: "End of a date range, YYYY-MM-DD, inclusive.",
      type: "string",
    },
    limit: {
      description: "Result count. Server default 20, capped at 100.",
      type: "number",
    },
    timezone: {
      description: "IANA timezone for any timezone-dependent fields server-side.",
      type: "string",
    },
    locale: {
      description: "Locale for any localized fields server-side.",
      type: "string",
    },
  }}
/>

## Returns

`Match[] | null | undefined`: `undefined` while the request is in flight,
`null` if it failed (no fallback data), otherwise the list. One-shot: this
doesn't push live updates the way `useLiveMatch`/`useOdds` do; call it
again (or remount) to refresh.
